programa {
  funcao inicio() {
    real base, altura, area,
    escreva ("escreva a base  do triangulo")
    leia(base)
    escreva ("escreva a altura do trianguo")
    leia(altura)
    area = (base*altura)/2 
    escreva("a area do triangulo é ", area)
  }
}
